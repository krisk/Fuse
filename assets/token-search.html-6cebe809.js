import{_ as a,M as n,p as t,q as o,R as e,t as i,N as r,a1 as c}from"./framework-f927af21.js";const p={},d={id:"token-search",tabindex:"-1"},l=e("a",{class:"header-anchor",href:"#token-search","aria-hidden":"true"},"#",-1),u=c(`<p>Token search splits multi-word queries into individual terms, fuzzy-matches each term independently using the Bitap algorithm, and ranks results using BM25-style IDF weighting.</p><h2 id="when-to-use-it" tabindex="-1"><a class="header-anchor" href="#when-to-use-it" aria-hidden="true">#</a> When to Use It</h2><p>The default fuzzy search treats the entire query as a single pattern. That works well for short, single-word lookups like <code>&quot;javscript&quot;</code> → <code>&quot;JavaScript&quot;</code>. But for multi-word queries like <code>&quot;javascript design patterns&quot;</code>, a single Bitap search hits the 32-character limit and can&#39;t match each word independently.</p><p>Token search is designed for these cases:</p><ul><li><strong>Search bars</strong> — users type natural multi-word queries like <code>&quot;react state management&quot;</code></li><li><strong>Document search</strong> — matching across titles, descriptions, or body text where multiple terms matter</li><li><strong>Autocomplete</strong> — ranking results by how many query terms they match, with rare terms weighted higher</li></ul><p>If your queries are typically one word or a short phrase, the default fuzzy search is simpler and faster.</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> fuse <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Fuse</span><span class="token punctuation">(</span>docs<span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">useTokenSearch</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span>
  <span class="token literal-property property">keys</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;title&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;author&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;description&#39;</span><span class="token punctuation">]</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>

fuse<span class="token punctuation">.</span><span class="token function">search</span><span class="token punctuation">(</span><span class="token string">&#39;javascrpt paterns&#39;</span><span class="token punctuation">)</span>
<span class="token comment">// → [{ item: { title: &#39;JavaScript Patterns&#39;, ... }, score: 0.12 }]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>All standard options work as before: <code>includeScore</code>, <code>includeMatches</code>, <code>keys</code> with weights, <code>threshold</code>, <code>limit</code>, <code>shouldSort</code>, etc.</p><h2 id="how-it-works" tabindex="-1"><a class="header-anchor" href="#how-it-works" aria-hidden="true">#</a> How It Works</h2><ol><li><p><strong>Tokenization</strong> — The query is split into individual words using word boundary matching (<code>\\b\\w+\\b</code>). Each word becomes a separate fuzzy search.</p></li><li><p><strong>Per-term fuzzy matching</strong> — Each term is matched against each field using the Bitap algorithm with <code>ignoreLocation: true</code>, so terms can appear anywhere in the field. This means multi-word queries are no longer limited by the 32-character Bitap pattern cap.</p></li><li><p><strong>IDF weighting</strong> — An inverted index is built at construction time. The IDF weight for each term uses the BM25 formula:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>idf = log(1 + (fieldCount - docFreq + 0.5) / (docFreq + 0.5))
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Rare terms (appearing in fewer documents) are weighted higher than common terms. A match on a distinctive word contributes more to the score than a match on a ubiquitous one.</p></li><li><p><strong>Score combination</strong> — Per-term scores are combined additively with IDF weights, then normalized to Fuse&#39;s 0–1 range (0 = perfect match).</p></li></ol><h2 id="key-behaviors" tabindex="-1"><a class="header-anchor" href="#key-behaviors" aria-hidden="true">#</a> Key Behaviors</h2><ul><li><strong>Partial matching</strong> — A document matching 2 of 3 query terms still returns, but ranks lower than one matching all 3.</li><li><strong>Word order independence</strong> — <code>&quot;patterns javascript&quot;</code> and <code>&quot;javascript patterns&quot;</code> produce identical results.</li><li><strong>Typo tolerance per term</strong> — Each term is fuzzy-matched independently, so typos in any word are tolerated.</li><li><strong>Long queries work</strong> — A 6-word query runs 6 independent Bitap searches, each within the 32-char limit.</li></ul><h2 id="tips" tabindex="-1"><a class="header-anchor" href="#tips" aria-hidden="true">#</a> Tips</h2><ul><li>Use <code>threshold</code> to control fuzziness. The default <code>0.6</code> is permissive — for tighter matching, try <code>0.3</code> or <code>0.4</code>.</li><li>Use <code>limit</code> when you only need the top N results. This also improves performance via heap-based selection.</li><li>Key weights still apply on top of token search scoring, so you can boost title matches over body matches.</li></ul><h2 id="dynamic-collection-updates" tabindex="-1"><a class="header-anchor" href="#dynamic-collection-updates" aria-hidden="true">#</a> Dynamic Collection Updates</h2><p>The inverted index is maintained as you modify the collection:</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> fuse <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Fuse</span><span class="token punctuation">(</span>docs<span class="token punctuation">,</span> <span class="token punctuation">{</span> <span class="token literal-property property">useTokenSearch</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span> <span class="token literal-property property">keys</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;title&#39;</span><span class="token punctuation">]</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>

<span class="token comment">// Adding a document updates the inverted index</span>
fuse<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span><span class="token punctuation">{</span> <span class="token literal-property property">title</span><span class="token operator">:</span> <span class="token string">&#39;New Book&#39;</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>

<span class="token comment">// Removing documents also updates the index</span>
fuse<span class="token punctuation">.</span><span class="token function">remove</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token parameter">doc</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> doc<span class="token punctuation">.</span>title <span class="token operator">===</span> <span class="token string">&#39;Old Book&#39;</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="performance" tabindex="-1"><a class="header-anchor" href="#performance" aria-hidden="true">#</a> Performance</h2><p>Benchmarked on randomly generated documents with 2 keys (title + body):</p><table><thead><tr><th>Metric</th><th>100 docs</th><th>1,000 docs</th><th>5,000 docs</th></tr></thead><tbody><tr><td>Index creation overhead</td><td>2.5x</td><td>5.2x</td><td>5.5x</td></tr><tr><td>Single-term search</td><td>1.8x</td><td>1.8x</td><td>1.7x</td></tr><tr><td>Multi-term search</td><td>1.3x</td><td>1.3x</td><td>1.2x</td></tr></tbody></table><p>Index creation is a one-time cost (46ms for 5,000 docs). Search overhead is 1.2–1.8x depending on query complexity, primarily because each query term runs its own Bitap search. The inverted index lookup itself is O(1) per term.</p><h2 id="availability" tabindex="-1"><a class="header-anchor" href="#availability" aria-hidden="true">#</a> Availability</h2><p>Token search is available in the <strong>full build</strong> only (<code>fuse.js</code> / <code>fuse.mjs</code>). It is not included in the basic build to keep bundle size small. Using <code>useTokenSearch: true</code> with the basic build throws an error.</p><p>To use token search, use the full build.</p>`,24);function h(m,k){const s=n("Badge");return t(),o("div",null,[e("h1",d,[l,i(" Token Search "),r(s,{type:"tip",text:"New"})]),u])}const v=a(p,[["render",h],["__file","token-search.html.vue"]]);export{v as default};
