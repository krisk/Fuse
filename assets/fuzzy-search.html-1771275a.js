import{_ as c,M as n,p as r,q as l,R as a,t as e,N as s,a1 as t}from"./framework-188d6338.js";const p={},d=a("h1",{id:"fuzzy-search",tabindex:"-1"},[a("a",{class:"header-anchor",href:"#fuzzy-search","aria-hidden":"true"},"#"),e(" Fuzzy Search")],-1),h={href:"https://en.wikipedia.org/wiki/Bitap_algorithm",target:"_blank",rel:"noopener noreferrer"},u=a("em",null,"approximately",-1),m=t(`<div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> fuse <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Fuse</span><span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token string">&#39;apple&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;banana&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;orange&#39;</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">includeScore</span><span class="token operator">:</span> <span class="token boolean">true</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>

fuse<span class="token punctuation">.</span><span class="token function">search</span><span class="token punctuation">(</span><span class="token string">&#39;aple&#39;</span><span class="token punctuation">)</span>
<span class="token comment">// [{ item: &#39;apple&#39;, refIndex: 0, score: 0.25 }]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="how-it-works" tabindex="-1"><a class="header-anchor" href="#how-it-works" aria-hidden="true">#</a> How It Works</h2><p>The Bitap algorithm computes the edit distance between the search pattern and each position in the text. The implementation uses bitwise operations for speed, with a pattern length limit of 32 characters per search term.</p><p>The algorithm produces a <strong>fuzziness score</strong> between 0 and 1:</p><ul><li><code>0</code> — perfect match</li><li><code>1</code> — complete mismatch</li></ul><p>This score is then combined with key weight and field-length norm to produce the final relevance score.</p><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>See <a href="/articles/how-fuzzy-search-works">How Fuzzy Search Works</a> for an interactive walkthrough of edit distance.</p></div><h2 id="controlling-fuzziness" tabindex="-1"><a class="header-anchor" href="#controlling-fuzziness" aria-hidden="true">#</a> Controlling Fuzziness</h2><p>Three options work together to determine what counts as a match:</p><h4 id="threshold" tabindex="-1"><a class="header-anchor" href="#threshold" aria-hidden="true">#</a> <code>threshold</code></h4><ul><li>Default: <code>0.6</code></li></ul><p>The cutoff for the fuzziness score. A threshold of <code>0.0</code> requires a perfect match. A threshold of <code>1.0</code> matches anything.</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// Strict matching</span>
<span class="token keyword">const</span> fuse <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Fuse</span><span class="token punctuation">(</span>list<span class="token punctuation">,</span> <span class="token punctuation">{</span> <span class="token literal-property property">keys</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;title&#39;</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token literal-property property">threshold</span><span class="token operator">:</span> <span class="token number">0.2</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>

<span class="token comment">// Permissive matching</span>
<span class="token keyword">const</span> fuse <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Fuse</span><span class="token punctuation">(</span>list<span class="token punctuation">,</span> <span class="token punctuation">{</span> <span class="token literal-property property">keys</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;title&#39;</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token literal-property property">threshold</span><span class="token operator">:</span> <span class="token number">0.8</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="location" tabindex="-1"><a class="header-anchor" href="#location" aria-hidden="true">#</a> <code>location</code></h4><ul><li>Default: <code>0</code></li></ul><p>The expected position of the pattern in the text. The algorithm penalizes matches that are far from this position.</p><h4 id="distance" tabindex="-1"><a class="header-anchor" href="#distance" aria-hidden="true">#</a> <code>distance</code></h4><ul><li>Default: <code>100</code></li></ul><p>How far from <code>location</code> a match can be before being penalized to the point of exclusion. The effective search window is:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>threshold × distance = maximum offset from location
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>With defaults (<code>threshold: 0.6</code>, <code>distance: 100</code>), the pattern must appear within 60 characters of position 0 to match. For example, searching for <code>&quot;zero&quot;</code> in <code>&quot;Fuse.js is a powerful, lightweight fuzzy-search library, with zero dependencies&quot;</code> would <strong>not</strong> match — <code>&quot;zero&quot;</code> appears at index 62, just outside the window.</p><h4 id="ignorelocation" tabindex="-1"><a class="header-anchor" href="#ignorelocation" aria-hidden="true">#</a> <code>ignoreLocation</code></h4><ul><li>Default: <code>false</code></li></ul><p>When <code>true</code>, disables location-based scoring entirely. The pattern can appear anywhere in the text and still match. This is useful when you don&#39;t know or care where the match might be.</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> fuse <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Fuse</span><span class="token punctuation">(</span>list<span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">keys</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;description&#39;</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
  <span class="token literal-property property">ignoreLocation</span><span class="token operator">:</span> <span class="token boolean">true</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>The default options effectively search only the first ~60 characters of each field. If your data has long text fields, either increase <code>distance</code>, or set <code>ignoreLocation: true</code>.</p></div><p>Try adjusting <code>threshold</code>, <code>location</code>, and <code>distance</code> below to see how they interact.</p>`,27),f=t('<h2 id="other-options" tabindex="-1"><a class="header-anchor" href="#other-options" aria-hidden="true">#</a> Other Options</h2><h4 id="iscasesensitive" tabindex="-1"><a class="header-anchor" href="#iscasesensitive" aria-hidden="true">#</a> <code>isCaseSensitive</code></h4><ul><li>Default: <code>false</code></li></ul><p>When <code>true</code>, comparisons are case-sensitive.</p><h4 id="ignorediacritics" tabindex="-1"><a class="header-anchor" href="#ignorediacritics" aria-hidden="true">#</a> <code>ignoreDiacritics</code></h4><ul><li>Default: <code>false</code></li></ul><p>When <code>true</code>, comparisons ignore diacritics (accents). For example, <code>é</code> matches <code>e</code>.</p><h4 id="findallmatches" tabindex="-1"><a class="header-anchor" href="#findallmatches" aria-hidden="true">#</a> <code>findAllMatches</code></h4><ul><li>Default: <code>false</code></li></ul><p>When <code>true</code>, the algorithm continues searching to the end of the text even after finding a perfect match. Useful when you need all match indices for highlighting.</p><h4 id="minmatchcharlength" tabindex="-1"><a class="header-anchor" href="#minmatchcharlength" aria-hidden="true">#</a> <code>minMatchCharLength</code></h4><ul><li>Default: <code>1</code></li></ul><p>Only return matches whose length exceeds this value. Set to <code>2</code> to ignore single-character matches.</p><h2 id="scoring" tabindex="-1"><a class="header-anchor" href="#scoring" aria-hidden="true">#</a> Scoring</h2><p>The final relevance score for a result combines three factors:</p><ul><li><strong>Fuzziness score</strong> — the raw Bitap score described above</li><li><strong>Key weight</strong> — optional per-key <code>weight</code> (defaults to <code>1</code>). Higher weight = more influence on ranking. Weights are normalized internally.</li><li><strong>Field-length norm</strong> — shorter fields rank higher. A match in a title is more significant than the same match in a long description.</li></ul><p>Scores range from <code>0</code> (perfect match) to <code>1</code> (complete mismatch). Enable with <code>includeScore: true</code>.</p><h4 id="ignorefieldnorm" tabindex="-1"><a class="header-anchor" href="#ignorefieldnorm" aria-hidden="true">#</a> <code>ignoreFieldNorm</code></h4><ul><li>Default: <code>false</code></li></ul><p>When <code>true</code>, field length has no effect on scoring. Useful when you only care whether a term exists, not how prominent it is.</p><h4 id="fieldnormweight" tabindex="-1"><a class="header-anchor" href="#fieldnormweight" aria-hidden="true">#</a> <code>fieldNormWeight</code></h4><ul><li>Default: <code>1</code></li></ul><p>Adjusts the strength of the field-length norm. <code>0</code> is equivalent to ignoring it. <code>0.5</code> reduces the effect. <code>2.0</code> amplifies it.</p>',23);function g(k,v){const o=n("ExternalLinkIcon"),i=n("SearchWindowDemo");return r(),l("div",null,[d,a("p",null,[e("Fuse.js uses a modified "),a("a",h,[e("Bitap algorithm"),s(o)]),e(" for approximate string matching. It finds strings that are "),u,e(" equal to a given pattern — tolerating typos, transpositions, and missing characters.")]),m,s(i),f])}const w=c(p,[["render",g],["__file","fuzzy-search.html.vue"]]);export{w as default};
