import{_ as h,M as s,p as l,q as p,N as i,R as e,t,a1 as a}from"./framework-f927af21.js";const m={},u=e("h1",{id:"how-fuzzy-search-works",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#how-fuzzy-search-works","aria-hidden":"true"},"#"),t(" How Fuzzy Search Works")],-1),g=a('<p>When you search for &quot;javscript&quot; and Fuse.js returns &quot;JavaScript&quot;, it&#39;s using <strong>approximate string matching</strong> — finding strings that are <em>close</em> to your query even when they don&#39;t match exactly.</p><p>The core idea is <strong>edit distance</strong>: the minimum number of single-character operations (insertions, deletions, substitutions) needed to transform one string into another.</p><p>For example, turning &quot;javscript&quot; into &quot;javascript&quot; requires:</p><ul><li><strong>Substitute</strong> <code>o</code> → <code>a</code> (at position 2)</li><li><strong>Insert</strong> <code>a</code> (at position 4)</li><li><strong>Insert</strong> <code>s</code> (at position 10)</li></ul><p>That&#39;s an edit distance of 3. The lower the distance relative to the string length, the better the match.</p><h2 id="edit-distance-demo" tabindex="-1"><a class="header-anchor" href="#edit-distance-demo" aria-hidden="true">#</a> Edit Distance Demo</h2><p>Step through the alignment between any two strings to see how edit distance is computed.</p>',7),v=a('<h2 id="from-edit-distance-to-fuzziness-score" tabindex="-1"><a class="header-anchor" href="#from-edit-distance-to-fuzziness-score" aria-hidden="true">#</a> From Edit Distance to Fuzziness Score</h2><p>Fuse.js converts edit distance into a <strong>fuzziness score</strong> between 0 and 1:</p><ul><li><code>0</code> — perfect match (edit distance of 0)</li><li><code>1</code> — complete mismatch</li></ul><p>This score is then combined with other factors like key weight and field-length normalization to produce the final relevance ranking.</p><p>The <code>threshold</code> option (default <code>0.6</code>) controls the cutoff — results with a score above the threshold are excluded. Drag the slider to see how it affects which results make the cut:</p>',5),b=e("p",null,[t("See "),e("a",{href:"/fuzzy-search"},"Fuzzy Search"),t(" for details on configuring threshold, distance, location, and scoring.")],-1),f=e("h2",{id:"under-the-hood-bitap",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#under-the-hood-bitap","aria-hidden":"true"},"#"),t(" Under the Hood: Bitap")],-1),x={href:"https://en.wikipedia.org/wiki/Bitap_algorithm",target:"_blank",rel:"noopener noreferrer"},w=a(`<div class="custom-container tip"><p class="custom-container-title">Why 32 characters?</p><p>The algorithm encodes the pattern as a bitmask and uses bitwise operators (<code>&lt;&lt;</code>, <code>|</code>, <code>&amp;</code>) to process all positions in a single CPU operation. JavaScript&#39;s bitwise operators work on 32-bit integers — that&#39;s the limit. Longer patterns are split into 32-character chunks and searched independently.</p></div><p>Here&#39;s how it works for <strong>exact matching</strong> (zero errors):</p><ol><li><p><strong>Build bitmasks</strong> — for each character in the pattern, create a bitmask where bit <code>i</code> is <code>1</code> if the character matches position <code>i</code>, and <code>0</code> otherwise. Fuse.js does this via <code>createPatternAlphabet()</code>. For the pattern <code>test</code>:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>         t  e  s  t
mask[t]  1  0  0  1
mask[e]  0  1  0  0
mask[s]  0  0  1  0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>Initialize state</strong> — the state vector <code>R</code> starts as all <code>0</code>s (no matches yet).</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>     t  e  s  t
R =  0  0  0  0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>Scan the text</strong> — for each character, three things happen in a single bitwise operation:</p><ul><li><strong>Seed</strong> — a new potential match starts at the first pattern position</li><li><strong>Propagate</strong> — existing partial matches advance to the next position</li><li><strong>Kill</strong> — any position where the text character doesn&#39;t match the pattern is reset to <code>0</code></li></ul></li><li><p><strong>Match</strong> — when the bit for the last pattern position is <code>1</code>, the entire pattern has been matched.</p></li></ol><p>For example, searching for <code>test</code> in <code>&quot;xtest&quot;</code>:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>                 t  e  s  t
Start:      R =  0  0  0  0

Read &#39;x&#39;:   R =  0  0  0  0   ← &#39;x&#39; isn&#39;t in the pattern, seed killed
Read &#39;t&#39;:   R =  1  0  0  0   ← &#39;t&#39; matches position 0, partial match starts
Read &#39;e&#39;:   R =  0  1  0  0   ← &#39;e&#39; matches position 1, match continues
Read &#39;s&#39;:   R =  0  0  1  0   ← &#39;s&#39; matches position 2, still going
Read &#39;t&#39;:   R =  1  0  0  1   ← &#39;t&#39; matches position 3 → full match!
                                  (also starts a new match at position 0)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Step through it yourself:</p>`,6),_=a(`<h2 id="handling-typos-approximate-matching" tabindex="-1"><a class="header-anchor" href="#handling-typos-approximate-matching" aria-hidden="true">#</a> Handling Typos: Approximate Matching</h2><p>The demo above only shows exact matching. But Fuse.js is a <em>fuzzy</em> search library — it needs to handle typos, missing characters, and extra characters. It does this by running the Bitap algorithm multiple times, allowing one more error each pass.</p><p>Instead of a single state vector <code>R</code>, Fuse maintains multiple: <code>R0</code> (zero errors), <code>R1</code> (one error), <code>R2</code> (two errors), and so on. When a partial match dies in one level because the characters don&#39;t match, it can continue in the next level — counting that mismatch as an error:</p><ul><li><strong>Substitution</strong> — a partial match in <code>R0</code> hits a wrong character. Instead of dying, it continues in <code>R1</code> with one error charged.</li><li><strong>Deletion</strong> — a partial match skips a pattern position (a character is missing from the text). It continues in <code>R1</code>.</li><li><strong>Insertion</strong> — a partial match skips a text character (an extra character in the text). It continues in <code>R1</code>.</li></ul><p>For example, searching for <code>test</code> in <code>&quot;tset&quot;</code> (a transposition):</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>                   t  e  s  t
R0 (0 errors):     1  0  0  0   ← &#39;t&#39; matches, but &#39;s&#39; ≠ &#39;e&#39; — exact match dies
R1 (1 error):      1  1  0  0   ← the mismatch continues here as 1 error
R2 (2 errors):     1  1  1  1   ← another mismatch, 2 errors — still matches!
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The algorithm stops adding error levels when the score exceeds the <code>threshold</code>. This is how <code>threshold</code> controls fuzziness — a lower threshold means fewer error levels are tried, requiring closer matches.</p>`,7);function z(y,k){const o=s("PublishDate"),n=s("FuzzyMatchDemo"),r=s("ThresholdDemo"),c=s("ExternalLinkIcon"),d=s("BitapDemo");return l(),p("div",null,[u,i(o,{date:"2026-04-09"}),g,i(n),v,i(r),b,f,e("p",null,[t("Fuse.js doesn't use a traditional dynamic programming table to compute edit distance. Instead, it uses the "),e("a",x,[t("Bitap algorithm"),i(c)]),t(", which encodes the pattern as bitmasks and uses bitwise operations to check all character positions in parallel. This makes it fast — especially for short patterns.")]),w,i(d),_])}const F=h(m,[["render",z],["__file","how-fuzzy-search-works.html.vue"]]);export{F as default};
